version = 1
name = "known-folders-rs"

[setup]
script = "./scripts/setup_codex_worktree.sh"

[setup.win32]
script = """
Write-Error "known-folders-rs worktree setup is supported on macOS and Linux only."
exit 69
"""
